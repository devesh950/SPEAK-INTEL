# SpeakIntel AI Backend

FastAPI backend for the SpeakIntel AI Communication Coach.

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Environment Variables

Copy `.env.example` to `.env` and fill in values.
